import { useMemo } from "react";

// const changeCount = useCallback(() => {
//     setCount(1);
// }, []);

// const elem = useMemo(() => props.hugeArray.find(el => el.show),
//     [props.hugeArray]
// );